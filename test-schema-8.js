import { createShapeId } from '@tldraw/tldraw';
import { TLShapeId } from '@tldraw/tldraw';
// We just need the default props of a geo shape.
import { geoShapeProps } from '@tldraw/tldraw';
console.log(geoShapeProps);
