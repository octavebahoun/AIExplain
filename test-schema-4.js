import { geoShapeProps, textShapeProps, TLRichText } from '@tldraw/tldraw';
console.log('geo text:', geoShapeProps.text);
console.log('geo richText:', geoShapeProps.richText);
console.log('text text:', textShapeProps.text);
console.log('text richText:', textShapeProps.richText);
